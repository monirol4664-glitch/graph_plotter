name: math_suite
description: Complete Mathematics Suite for Students
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  math_expressions: ^2.0.4
  cupertino_icons: ^1.0.2
  flutter_math: ^0.5.0
  vector_math: ^2.1.4
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
